<div class="mt-4">
  <div class="row">
    <div class="col-lg-3">
      <button>Hoc Ngay</button>
    </div>
    <div class="col-lg-9">
      <h1>{{course.name}}</h1>
      <img src="{{course.image}}" alt="{{course.image}}" />
      <p>{{course.description}}</p>
    </div>
  </div>
</div>